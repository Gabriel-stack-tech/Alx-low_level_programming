#include "main"
#include <stdio.h>
/**
 * function that computers the absolute value
 * argument of the function
 * Return:0
 */ 
int -abs(int c)
{
	if ( c > o || c == 0)
	{
		return(1);
	}
	else
		return(c * -1);
}

